version: '3.8'

services:
  server:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn netprobe.server:app --host 0.0.0.0 --port 8000

  agent:
    build: .
    volumes:
      - .:/app
    depends_on:
      - server
    command: python -u netprobe/agent.py
